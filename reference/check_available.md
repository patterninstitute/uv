# Require uv to be available

Aborts with an informative message if the `uv` executable cannot be
found.

## Usage

``` r
check_available()
```

## Value

Invisibly returns `NULL` if `uv` is available.

## Examples

``` r
if (FALSE) check_available() # \dontrun{}
```
