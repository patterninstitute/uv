% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_lock.R
\name{check_lock}
\alias{check_lock}
\title{Check if lockfile is up-to-date}
\usage{
check_lock(path = ".")
}
\arguments{
\item{path}{Directory inside a uv project.}
}
\value{
\code{TRUE} if the lockfile exists and is up-to-date with \code{pyproject.toml},
\code{FALSE} otherwise.
}
\description{
Checks whether the \code{uv.lock} file is consistent with \code{pyproject.toml}.
This is a non-modifying check that returns \code{TRUE} or \code{FALSE}.
}
\details{
This function runs \verb{uv lock --check} which verifies that:
\itemize{
\item A \code{uv.lock} file exists
\item The lockfile is consistent with the current \code{pyproject.toml}
}

If either condition is not met, the function returns \code{FALSE}.
Unlike \code{\link[=lock]{lock()}}, this function never modifies the lockfile.
}
\examples{
\dontshow{if (uv::available()) withAutoprint(\{ # examplesIf}
with_temp_project({
  init()

  # No lockfile yet
  check_lock()

  # Create lockfile
  lock()

  # Now it's up-to-date
  check_lock()

  # Add a dependency (lockfile becomes stale)
  add("requests")
  check_lock()

  # Update the lockfile
  lock()
  check_lock()
})
\dontshow{\}) # examplesIf}
}
\seealso{
\itemize{
\item \code{\link[=lock]{lock()}} to create or update the lockfile
\item \code{\link[=lock_exists]{lock_exists()}} to check only for existence
}
}
