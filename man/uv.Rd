% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uv.R
\name{uv}
\alias{uv}
\title{Call uv using structured command semantics}
\usage{
uv(
  verb = "help",
  namespace = NULL,
  .args = character(),
  .flags = character(),
  .path = ".",
  .require_project = FALSE,
  .require_pyproject = FALSE,
  .require_lock = FALSE,
  .echo = TRUE,
  .error_on_status = FALSE,
  .dry_run = FALSE
)
}
\arguments{
\item{verb}{A single character string giving the uv subcommand to execute
(e.g. \code{"sync"}, \code{"install"}, \code{"find"}).}

\item{namespace}{Optional command namespace (e.g. \code{"pip"}, \code{"python"},
\code{"tool"}). If \code{NULL}, a top-level uv command is invoked.}

\item{.args}{Character vector of positional arguments passed to uv.}

\item{.flags}{Character vector of option flags passed to uv
(e.g. \code{"--upgrade"}, \verb{"--group", "dev"}).}

\item{.path}{Directory used to resolve the uv project root.}

\item{.require_project}{Logical; if \code{TRUE}, requires both \code{pyproject.toml}
and \code{uv.lock} to be present.}

\item{.require_pyproject}{Logical; if \code{TRUE}, requires only \code{pyproject.toml}
to be present. Use for commands that can run before a lockfile exists.}

\item{.require_lock}{Logical; whether the command requires an existing
\code{uv.lock} file.}

\item{.echo}{Logical; whether to stream uv output to the console.}

\item{.error_on_status}{Logical; whether to error if uv exits with a non-zero
status code.}

\item{.dry_run}{Logical; if \code{TRUE}, return the constructed command without
executing it.}
}
\value{
A \code{processx} result object, or (if \code{.dry_run = TRUE}) the constructed command
as a character vector with class \code{"uv_command"}.
}
\description{
High-level interface to the \code{uv} command-line tool.

This function provides a structured API over uv's command grammar, separating
uv-level arguments from R-level control parameters.

uv commands follow the general form:

\if{html}{\out{<div class="sourceCode">}}\preformatted{uv [namespace] verb [arguments] [flags]
}\if{html}{\out{</div>}}

This grammar is made explicit via the \code{namespace}, \code{verb}, \code{.args}, and
\code{.flags} parameters.

R-level control parameters are prefixed with \code{.} to clearly distinguish them
from arguments passed to uv.
}
\examples{
\dontshow{if (uv::available()) withAutoprint(\{ # examplesIf}
with_temp_project({

  # 1. Initialise project
  uv("init")
  fs::file_info("pyproject.toml")

  # 2. Declare a dependency
  uv("add", .args = "requests")
  readLines("pyproject.toml")

  # 3. Resolve and lock dependencies
  uv("lock")
  fs::file_info("uv.lock")

  # 4. Materialise environment
  uv("sync", .require_lock = TRUE)
  fs::dir_ls(".venv")

  # 5. Prove the package is importable
  uv("run", .args = c("python", "-c", "import requests; print(requests.__version__)"))
})
\dontshow{\}) # examplesIf}
}
