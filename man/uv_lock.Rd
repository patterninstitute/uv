% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lock.R
\name{uv_lock}
\alias{uv_lock}
\title{Lock project dependencies using uv (engine)}
\usage{
uv_lock(
  check_up_to_date = FALSE,
  check_exists = FALSE,
  upgrade = FALSE,
  upgrade_package = NULL,
  script = NULL,
  .path = ".",
  .dry_run = FALSE
)
}
\arguments{
\item{check_up_to_date}{Logical; if \code{TRUE}, check whether \code{uv.lock} is
consistent with \code{pyproject.toml} without modifying it.}

\item{check_exists}{Logical; if \code{TRUE}, assert that a \code{uv.lock} file exists
without checking whether it is up-to-date.}

\item{upgrade}{Logical; if \code{TRUE}, upgrade all locked packages to the latest
versions allowed by project constraints.}

\item{upgrade_package}{Optional package name (or \code{pkg==version}) to upgrade
while leaving all other locked versions unchanged.}

\item{script}{Optional path to a Python script to lock instead of a project.}

\item{.path}{Directory inside a uv project.}

\item{.dry_run}{Logical; if \code{TRUE}, return the command without executing it.}
}
\value{
A \code{processx} result object, or a \code{uv_command} if \code{.dry_run = TRUE}.
}
\description{
Resolves and validates the dependency state of a uv project.
}
\details{
This is a low-level wrapper around \verb{uv lock} that exposes the core semantic
operations needed by the high-level \code{\link[=lock]{lock()}} function. It is intended for
internal use and advanced workflows.
}
\keyword{internal}
