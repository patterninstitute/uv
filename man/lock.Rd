% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lock.R
\name{lock}
\alias{lock}
\title{Resolve and validate project dependencies}
\usage{
lock(
  check_up_to_date = FALSE,
  check_exists = FALSE,
  upgrade = FALSE,
  upgrade_package = NULL,
  path = ".",
  verbose = TRUE
)
}
\arguments{
\item{check_up_to_date}{Logical; if \code{TRUE}, check whether the lockfile is
consistent with \code{pyproject.toml} without modifying it.}

\item{check_exists}{Logical; if \code{TRUE}, assert that a lockfile exists.}

\item{upgrade}{Logical; if \code{TRUE}, upgrade all locked packages.}

\item{upgrade_package}{Optional package name (or \code{pkg==version}) to upgrade.}

\item{path}{Directory inside a uv project.}

\item{verbose}{Whether to be chatty.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effects.
}
\description{
Manages the dependency state of a uv project by creating, validating, or
updating the \code{uv.lock} file.
}
\details{
This function models dependency management as a project state operation,
rather than a file operation. It can be used to:
\itemize{
\item create or update the lockfile,
\item assert that a lockfile exists,
\item assert that the lockfile is up-to-date,
\item upgrade locked package versions.
}
}
\examples{
\dontshow{if (uv::available()) withAutoprint(\{ # examplesIf}
with_temp_project({

  init()

  # Create lockfile
  lock()

  # Assert that a lockfile exists
  lock(check_exists = TRUE)

  # Assert that it is up-to-date
  lock(check_up_to_date = TRUE)

  # Upgrade all dependencies
  lock(upgrade = TRUE)
})
\dontshow{\}) # examplesIf}
}
