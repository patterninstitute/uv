% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add.R
\name{add}
\alias{add}
\title{Add dependencies to a uv project}
\usage{
add(packages, group = NULL, path = ".", verbose = TRUE)
}
\arguments{
\item{packages}{Character vector of package specifications to add.}

\item{group}{Optional dependency group (e.g. \code{"dev"}).}

\item{path}{Directory inside a uv project.}

\item{verbose}{Whether to be chatty.}
}
\value{
Invisibly returns \code{NULL}. Called for its side effects.
}
\description{
Declares one or more dependencies in \code{pyproject.toml} using \verb{uv add}.
This updates project metadata but does not resolve or install packages.
After adding dependencies, run \code{lock()} and then \code{sync()}.
}
\examples{
\dontshow{if (uv::available()) withAutoprint(\{ # examplesIf}
with_temp_project({

  init()

  # Add a runtime dependency
  add("requests")
  readLines("pyproject.toml")

  # Add a development dependency
  add("pytest", group = "dev")
  readLines("pyproject.toml")
})
\dontshow{\}) # examplesIf}
}
