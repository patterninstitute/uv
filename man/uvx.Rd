% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uv.R
\name{uvx}
\alias{uvx}
\title{Run a Python tool in an isolated environment using uvx}
\usage{
uvx(tool, args = character(), flags = character(), quiet = FALSE)
}
\arguments{
\item{tool}{A single character string. The tool (package) to run
(e.g. \code{"ruff"}, \code{"black"}, \code{"pycowsay"}).}

\item{args}{Character vector of arguments passed to the tool.}

\item{flags}{Character vector of uv flags passed to \verb{uv tool run}
(e.g. \verb{"--from", "ruff==0.6.2"}).}

\item{quiet}{Logical; whether to suppress uv output.}
}
\value{
A \code{processx} result object, returned invisibly.
}
\description{
Runs a command-line tool provided by a Python package in an ephemeral,
isolated environment.
}
\details{
This is a thin wrapper around \verb{uv tool run} (i.e. \code{uvx}).
}
\examples{
\dontshow{if (uv::available()) withAutoprint(\{ # examplesIf}
uvx("ruff", args = c("--version"), quiet = TRUE)
\dontshow{\}) # examplesIf}
}
