% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/install.R
\name{install_uv}
\alias{install_uv}
\title{Install the uv Python package manager}
\usage{
install_uv(version = NULL, quiet = FALSE)
}
\arguments{
\item{version}{Character scalar. A specific uv release to install, e.g.
\code{"0.6.1"}. When \code{NULL} (the default) the latest release is installed.}

\item{quiet}{Logical. Suppress installer output. Default \code{FALSE}.}
}
\value{
Invisibly returns the path to the installed \code{uv} binary.
}
\description{
Downloads and runs the official uv installer for the current platform.
On Unix and macOS the installer script is fetched from
\verb{https://astral.sh/uv/install.sh} and piped to \code{sh}. On Windows it is
fetched from \verb{https://astral.sh/uv/install.ps1} and executed with
\code{powershell}.
}
\details{
The installer places the \code{uv} binary in \verb{~/.local/bin} (Unix/macOS) or
\verb{\%USERPROFILE\%\\.local\\bin} (Windows) and does not require administrator
privileges or any system package manager.
}
\examples{
\dontrun{
install_uv()
install_uv(version = "0.6.1")
}

}
\seealso{
\code{\link[=available]{available()}}, \code{\link[=uv_version]{uv_version()}}
}
