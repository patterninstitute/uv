% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pip_install.R
\name{pip_install}
\alias{pip_install}
\title{Install Python packages using uv}
\usage{
pip_install(packages, quiet = FALSE)
}
\arguments{
\item{packages}{Character vector of package specifications accepted by
\verb{pip install} (e.g. \code{"requests"}, \code{"numpy>=2.0"}).}

\item{quiet}{Logical; whether to suppress uv output.}
}
\value{
A \code{processx} result object, returned invisibly.
}
\description{
Installs one or more Python packages into the uv-managed environment for the
current uv project.
}
\details{
This is a thin wrapper around \verb{uv pip install}. It must be called from within
a uv project.
}
\examples{
\dontshow{if (uv::available()) withAutoprint(\{ # examplesIf}
with_temp_project({
  init()
  pip_install("requests", quiet = TRUE)
})
\dontshow{\}) # examplesIf}
}
