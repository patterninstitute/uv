% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uv-path.R
\name{uv_path}
\alias{uv_path}
\title{Locate the uv executable}
\usage{
uv_path()
}
\value{
A character string giving the path to the \code{uv} executable, or \code{NULL}
if \code{uv} cannot be located.
}
\description{
Determines the path to the \code{uv} executable by checking a fixed and explicit
sequence of locations.
}
\details{
The lookup is performed in the following order:
\enumerate{
\item If the \code{UV} environment variable is set and non-empty, its value is used
as the path to \code{uv}.
\item Otherwise, the system \code{PATH} is searched using \code{Sys.which("uv")}.
\item If \code{uv} is still not found, the following OS-specific fallback locations
are checked, in order:
\itemize{
\item On Windows:
\itemize{
\item \verb{\%USERPROFILE\%\\\\.local\\\\bin\\\\uv.exe}
\item \verb{\%LOCALAPPDATA\%\\\\uv\\\\uv.exe}
\item \verb{\%USERPROFILE\%\\\\.cargo\\\\bin\\\\uv.exe}
}
\item On macOS and other Unix-like systems:
\itemize{
\item \verb{~/.local/bin/uv}
\item \verb{~/.cargo/bin/uv}
}
}
}

If none of these checks succeed, \code{NULL} is returned.

Setting the \code{UV} environment variable allows users to explicitly control
which \code{uv} executable is used. This is useful when multiple installations
are present or when \code{uv} is installed in a non-standard location that is not
on the system \code{PATH}.
}
\examples{
\dontrun{uv_path()}

}
\seealso{
\code{\link[=available]{available()}}, \code{\link[=uv_version]{uv_version()}}, \code{\link[=install_uv]{install_uv()}}
}
